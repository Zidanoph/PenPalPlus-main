# Base URL for the PenPal+ API.
# Leave as /api to use the Vite dev proxy (recommended for local dev).
# In production set this to your API origin, e.g. https://api.penpal.app
VITE_API_BASE=/api

# Only used by vite.config.js for the dev proxy target.
VITE_PROXY_TARGET=http://127.0.0.1:8000
